import { defineAPI } from "rlib/server";

export default defineAPI({
  name: "footer",
  url: "/api/footer",
  async handler() {
    

    const data = {
    };

    return {
      jsx: <></>,
      data: data,
    };
  },
});
